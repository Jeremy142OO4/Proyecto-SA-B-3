import{useCallback}from'react';import{useParams}from'react-router-dom';import{EstadoCarga,EstadoError,EstadoVacio}from'../../../components/feedback/EstadoCarga';import{usarConsulta}from'../../../hooks/usarConsulta';import{servicioCuentas}from'../services/servicioCuentas';
const dinero=(v:number)=>new Intl.NumberFormat('es-GT',{style:'currency',currency:'GTQ'}).format(v/100);
export function PaginaDetalleCuenta(){const{idCuenta=''}=useParams();const consultarCuenta=useCallback(()=>servicioCuentas.buscar(idCuenta),[idCuenta]);const consultarMovimientos=useCallback(()=>servicioCuentas.movimientos(idCuenta),[idCuenta]);const cuenta=usarConsulta(consultarCuenta);const movimientos=usarConsulta(consultarMovimientos);if(cuenta.cargando)return <EstadoCarga/>;if(cuenta.error)return <EstadoError mensaje={cuenta.error}/>;return <><div className="hero-saldo"><span>{cuenta.datos?.tipoCuenta}</span><h2>{dinero(cuenta.datos?.saldoCentavos??0)}</h2><p>Cuenta •••• {cuenta.datos?.numeroCuenta.slice(-4)}</p></div><div className="titulo-seccion"><h2>Movimientos recientes</h2></div>{movimientos.cargando?<EstadoCarga/>:movimientos.error?<EstadoError mensaje={movimientos.error}/>:!movimientos.datos?.length?<EstadoVacio mensaje="Esta cuenta aún no tiene movimientos."/>:<div className="lista">{movimientos.datos.map(m=><article key={m.idMovimiento}><div><strong>{m.descripcion||m.tipoMovimiento}</strong><small>{new Date(m.fechaCreacion).toLocaleString('es-GT')}</small></div><span className={m.tipoMovimiento==='DEBITO'?'negativo':'positivo'}>{m.tipoMovimiento==='DEBITO'?'-':'+'}{dinero(m.montoCentavos)}</span></article>)}</div>}</>}
