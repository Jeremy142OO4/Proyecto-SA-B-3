import{useState,type FormEvent}from'react';import{useNavigate}from'react-router-dom';import{servicioPagos}from'../services/servicioPagos';
export function PaginaNuevoPago(){const navegar=useNavigate();const[enviando,setEnviando]=useState(false);const[error,setError]=useState('');async function enviar(e:FormEvent<HTMLFormElement>){e.preventDefault();setEnviando(true);setError('');const f=new FormData(e.currentTarget);try{await servicioPagos.crear({idCuentaOrigen:String(f.get('cuenta')),beneficiario:String(f.get('beneficiario')),concepto:String(f.get('concepto')),montoCentavos:Math.round(Number(f.get('monto'))*100),tipoPago:String(f.get('tipo'))as'INTERNO'|'EXTERNO'});navegar('/pagos')}catch(x){setError(x instanceof Error?x.message:'No fue posible enviar el pago')}finally{setEnviando(false)}}return <div className="panel-formulario formulario-ancho"><p>Procesamiento seguro</p><h2>Nuevo pago</h2>{error&&<div className="alerta error">{error}</div>}<form onSubmit={enviar}><label>Cuenta de origen<input name="cuenta" required placeholder="Identificador de la cuenta"/></label><label>Beneficiario<input name="beneficiario" required maxLength={150}/></label><label>Concepto<input name="concepto" required maxLength={255}/></label><div className="dos-columnas"><label>Monto en quetzales<input name="monto" required type="number" min="0.01" step="0.01"/></label><label>Tipo<select name="tipo"><option value="INTERNO">Interno</option><option value="EXTERNO">Externo</option></select></label></div><button disabled={enviando}>{enviando?'Enviando…':'Confirmar pago'}</button></form></div>}
